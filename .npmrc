# Ensure workspace packages get local node_modules (e.g. next in apps/frontend).
# Netlify's Next.js serverless bundle resolves next from the app directory; hoisting
# next-only-to-repo-root breaks runtime with "Cannot find module 'next/dist/...'".
install-strategy=nested
